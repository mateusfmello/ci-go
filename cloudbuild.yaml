steps:

- id: "Rodando Testes"
  name: golang
  args: ['test']

- id: "Rodando script"
  name: golang
  args: ['run']

- id: "Rodando Build"
  name: golang
  args: ['build']

images:
  - 'gcr.io/$PROJECT_ID/go-soma:latest'
  - 'gcr.io/$PROJECT_ID/go-soma:0.0.1'
